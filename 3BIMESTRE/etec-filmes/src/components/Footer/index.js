import styles from './Footer.module.css';

function Footer(){
    return(
        <footer className={styles.footer}>
            Desenvolvido por eo.paulinn &reg; 20k4
        </footer>
    );
}

export default Footer; 